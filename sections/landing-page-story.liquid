{%- comment -%}
  PRODUCT LANDING REQUIREMENTS (all implemented in THIS file — not in custom-landing-page.liquid):

  1) Founder video     → Settings: "Founder video" (upload) OR "Video embed code" (YouTube/Vimeo iframe).
  2) Video testimonials → Blocks: "Video testimonial" (embed iframe + quote + name).
  3) GBP reviews        → Settings: "Reviews widget embed code" (sync via third-party widget) +
                          Blocks: "Manual Google review" (optional backup).
  4) Single product     → Settings: "Featured product" + description + add to cart form.
  5) Brand story        → Settings: "Brand story" heading, image, richtext.
  6) Instagram / reels → Blocks: "Instagram / reel" (tile shape: Reel 9:16 or square post, cover image, URL, optional Reels badge — add one block per item).

  Template: page.landing.json (suffix "landing"). Legacy Anarkali: "landing-anarkali" (custom-landing-page).
{%- endcomment -%}

{%- style -%}
  .lps-{{ section.id }} {
    --lps-gap: 1.5rem;
    --lps-max: 1200px;
    --lps-radius: var(--block-border-radius, 8px);
    --lps-transition: 0.35s ease;
    --lps-accent: #8b4513;
    --lps-accent-mid: #c47020;
    --lps-bg-warm: #fdf8f3;
    --lps-text: #1a1a1a;
    overflow-x: hidden;
  }
  .lps-{{ section.id }} .lps-inner { max-width: var(--lps-max); margin: 0 auto; padding-left: 1.25rem; padding-right: 1.25rem; }

  /* Brand logo — top-left (matches custom landing page) */
  .lps-{{ section.id }} .lps-corner-logo {
    display: block;
    padding: 1rem 1.25rem 0;
  }
  .lps-{{ section.id }} .lps-corner-logo img {
    height: clamp(2.5rem, 5vw, 4rem);
    width: auto;
    max-width: 220px;
    object-fit: contain;
    display: block;
  }
  /* Brand title banner — centred gradient title + divider (matches custom landing) */
  .lps-{{ section.id }} .lps-brand-banner {
    padding: 0 1.25rem 1.5rem;
    text-align: center;
  }
  .lps-{{ section.id }} .lps-brand-banner__eyebrow {
    display: block;
    font-size: clamp(0.62rem, 1.1vw, 0.72rem);
    font-weight: 700;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    opacity: 0.45;
    margin-bottom: 0.75rem;
  }
  .lps-{{ section.id }} .lps-brand-banner__title {
    display: block;
    font-family: var(--font-heading-family, inherit);
    font-size: clamp(2.4rem, 7vw, 5rem);
    font-weight: var(--font-heading-weight, 700);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    line-height: 1.05;
    margin: 0 auto 1rem;
    background: linear-gradient(135deg, {{ section.settings.banner_color_start | default: '#8b4513' }} 0%, {{ section.settings.banner_color_end | default: '#c47020' }} 45%, {{ section.settings.banner_color_start | default: '#8b4513' }} 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .lps-{{ section.id }} .lps-brand-banner__divider {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.25rem;
    max-width: 520px;
    margin: 0 auto;
  }
  .lps-{{ section.id }} .lps-brand-banner__divider::before,
  .lps-{{ section.id }} .lps-brand-banner__divider::after {
    content: '';
    flex: 1;
    height: 1px;
    background: currentColor;
    opacity: 0.18;
  }
  .lps-{{ section.id }} .lps-brand-banner__tagline {
    font-size: clamp(0.65rem, 1.2vw, 0.75rem);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    opacity: 0.5;
    white-space: nowrap;
  }
  @media (max-width: 600px) {
    .lps-{{ section.id }} .lps-corner-logo img { height: 2.2rem; }
    .lps-{{ section.id }} .lps-brand-banner { padding: 2rem 1rem 1.5rem; }
    .lps-{{ section.id }} .lps-brand-banner__tagline { white-space: normal; text-align: center; }
  }
  .lps-{{ section.id }} .lps-section-title {
    text-align: center;
    font-size: clamp(1.5rem, 3vw, 2.2rem);
    font-weight: 700;
    line-height: 1.2;
    color: var(--lps-text);
    margin: 0 0 0.75rem;
  }
  .lps-{{ section.id }} .lps-section-sub {
    text-align: center;
    opacity: 0.65;
    font-size: 0.95rem;
    margin: 0 0 2.25rem;
    max-width: 42rem;
    margin-left: auto;
    margin-right: auto;
  }

  /* Hero — full-bleed, copy left (matches custom landing page) */
  .lps-{{ section.id }} .lps-hero {
    position: relative;
    min-height: 90vh;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    overflow: hidden;
  }
  .lps-{{ section.id }} .lps-hero__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: left 70%;
    z-index: 0;
    transition: transform 7s ease;
  }
  .lps-{{ section.id }} .lps-hero:hover .lps-hero__bg { transform: scale(1.02); }
  .lps-{{ section.id }} .lps-hero__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, rgba(0,0,0,0.72) 38%, rgba(0,0,0,0.18) 65%, transparent 100%);
    z-index: 1;
  }
  .lps-{{ section.id }} .lps-hero__content {
    position: relative;
    z-index: 2;
    text-align: left;
    color: #fff;
    padding: 3rem 2rem 3rem 4rem;
    max-width: 560px;
    margin-left: 2vw;
  }
  .lps-{{ section.id }} .lps-hero__eyebrow {
    display: inline-block;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    background: rgba(255,255,255,0.18);
    border: 1px solid rgba(255,255,255,0.4);
    border-radius: 50px;
    padding: 0.35rem 1rem;
    margin-bottom: 1.25rem;
  }
  .lps-{{ section.id }} .lps-hero__heading {
    font-size: clamp(2rem, 5vw, 3.4rem);
    font-weight: 800;
    line-height: 1.15;
    margin: 0 0 1rem;
    text-shadow: 0 2px 12px rgba(0,0,0,0.35);
    color: #fff;
  }
  .lps-{{ section.id }} .lps-hero__sub {
    font-size: clamp(0.95rem, 2vw, 1.15rem);
    margin: 0 0 0.75rem;
    opacity: 0.9;
    line-height: 1.6;
  }
  .lps-{{ section.id }} .lps-hero__proof {
    font-size: clamp(0.78rem, 1.4vw, 0.88rem);
    opacity: 0.75;
    margin: 0 0 2rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }
  .lps-{{ section.id }} .lps-hero__cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .lps-{{ section.id }} .lps-cta-primary {
    display: inline-block;
    padding: 1rem 2.2rem;
    font-size: clamp(0.9rem, 1.8vw, 1.05rem);
    font-weight: 700;
    border-radius: var(--lps-radius);
    background: #fff;
    color: #1a1a1a;
    text-decoration: none;
    letter-spacing: 0.02em;
    transition: background var(--lps-transition), transform var(--lps-transition);
    box-shadow: 0 4px 20px rgba(0,0,0,0.25);
  }
  .lps-{{ section.id }} .lps-cta-primary:hover {
    background: #f0f0f0;
    transform: translateY(-2px);
  }
  .lps-{{ section.id }} .lps-cta-secondary {
    display: inline-block;
    padding: 0.95rem 1.75rem;
    font-size: clamp(0.88rem, 1.6vw, 1rem);
    font-weight: 600;
    border-radius: var(--lps-radius);
    border: 2px solid rgba(255,255,255,0.75);
    color: #fff;
    text-decoration: none;
    transition: border-color var(--lps-transition), background var(--lps-transition);
  }
  .lps-{{ section.id }} .lps-cta-secondary:hover {
    background: rgba(255,255,255,0.12);
    border-color: #fff;
  }
  @media (max-width: 768px) {
    .lps-{{ section.id }} .lps-hero__overlay {
      background: linear-gradient(
        to top,
        rgba(0,0,0,0.88) 0%,
        rgba(0,0,0,0.55) 45%,
        rgba(0,0,0,0.18) 75%,
        transparent 100%
      );
    }
    .lps-{{ section.id }} .lps-hero__content {
      text-align: center;
      padding: 2rem 1.5rem 3rem;
      margin-left: 0;
      align-self: flex-end;
      margin-bottom: 0;
      max-width: 100%;
    }
    .lps-{{ section.id }} .lps-hero__eyebrow { display: none; }
    .lps-{{ section.id }} .lps-hero__heading { font-size: clamp(1.6rem, 6.5vw, 2.2rem); }
    .lps-{{ section.id }} .lps-hero__sub {
      font-size: clamp(0.82rem, 3vw, 0.95rem);
      opacity: 0.85;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .lps-{{ section.id }} .lps-hero__proof {
      font-size: clamp(0.72rem, 2.4vw, 0.82rem);
      justify-content: center;
      margin-bottom: 1.5rem;
    }
    .lps-{{ section.id }} .lps-hero__cta {
      justify-content: center;
      flex-direction: row;
      flex-wrap: nowrap;
      gap: 0.5rem;
    }
    .lps-{{ section.id }} .lps-hero .lps-cta-primary,
    .lps-{{ section.id }} .lps-hero .lps-cta-secondary {
      flex: 0 0 auto;
      width: auto;
      text-align: center;
      padding: 0.7rem 1.25rem;
      font-size: clamp(0.82rem, 3vw, 0.9rem);
      white-space: nowrap;
    }
  }

  /* Featured product — same card pattern as custom landing product grid */
  .lps-{{ section.id }} .lps-products {
    padding: 4.5rem 1.25rem;
    background: var(--lps-bg-warm);
    border-top: 1px solid rgba(139,69,19,0.1);
    border-bottom: 1px solid rgba(139,69,19,0.1);
  }
  .lps-{{ section.id }} .lps-products > .lps-inner > .lps-section-title { margin-bottom: 0.75rem; }
  .lps-{{ section.id }} .lps-prod-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: var(--lps-gap);
    max-width: var(--lps-max);
    margin: 0 auto;
    justify-items: center;
  }
  .lps-{{ section.id }} .lps-prod-grid--single {
    max-width: 340px;
  }
  .lps-{{ section.id }} .lps-prod-card {
    width: 100%;
    border-radius: var(--lps-radius);
    overflow: hidden;
    border: 1px solid rgba(0,0,0,0.08);
    transition: transform var(--lps-transition), box-shadow var(--lps-transition);
    display: flex;
    flex-direction: column;
    background: #fff;
  }
  .lps-{{ section.id }} .lps-prod-card:hover {
    transform: translateY(-6px) scale(1.01);
    box-shadow: 0 22px 48px rgba(0,0,0,0.13);
  }
  .lps-{{ section.id }} .lps-prod-card__thumb {
    aspect-ratio: 3 / 4;
    overflow: hidden;
    position: relative;
  }
  .lps-{{ section.id }} .lps-prod-card__thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.5s ease;
  }
  .lps-{{ section.id }} .lps-prod-card:hover .lps-prod-card__thumb img { transform: scale(1.07); }
  .lps-{{ section.id }} .lps-prod-card__badge {
    position: absolute;
    top: 0.75rem;
    left: 0.75rem;
    background: rgba(0,0,0,0.65);
    color: #fff;
    font-size: clamp(0.62rem, 1.1vw, 0.7rem);
    letter-spacing: 1px;
    text-transform: uppercase;
    padding: 0.25rem 0.6rem;
    border-radius: 4px;
    z-index: 2;
  }
  .lps-{{ section.id }} .lps-prod-card__sold-out {
    position: absolute;
    inset: 0;
    background: rgba(255,255,255,0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 0.85rem;
    letter-spacing: 1px;
    text-transform: uppercase;
  }
  .lps-{{ section.id }} .lps-prod-card__body {
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
    flex: 1;
    text-align: left;
  }
  .lps-{{ section.id }} .lps-prod-card__name {
    font-weight: 600;
    font-size: clamp(0.88rem, 1.5vw, 1rem);
    margin: 0;
    color: var(--lps-text);
  }
  .lps-{{ section.id }} .lps-prod-card__price {
    font-size: clamp(0.8rem, 1.4vw, 0.92rem);
    opacity: 0.75;
    margin: 0;
    color: var(--lps-text);
  }
  .lps-{{ section.id }} .lps-prod-card__price s { margin-right: 0.35rem; }
  .lps-{{ section.id }} .lps-prod-card__desc {
    font-size: clamp(0.8rem, 1.4vw, 0.88rem);
    line-height: 1.65;
    opacity: 0.82;
    margin: 0;
  }
  .lps-{{ section.id }} .lps-prod-card__form select {
    width: 100%;
    padding: 0.65rem 0.85rem;
    margin-bottom: 0.35rem;
    border-radius: var(--lps-radius);
    border: 1px solid rgba(0,0,0,0.15);
    font-size: 0.95rem;
    background: #fff;
  }
  .lps-{{ section.id }} .lps-prod-card__btn { margin-top: auto; }
  .lps-{{ section.id }} .lps-prod-card__link {
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--lps-accent);
    text-decoration: underline;
    text-underline-offset: 3px;
    margin-top: 0.35rem;
  }
  @media (max-width: 600px) {
    .lps-{{ section.id }} .lps-prod-grid { grid-template-columns: 1fr 1fr; }
    .lps-{{ section.id }} .lps-prod-grid--single { grid-template-columns: 1fr; max-width: 100%; }
  }
  @media (max-width: 380px) {
    .lps-{{ section.id }} .lps-prod-grid { grid-template-columns: 1fr; }
  }

  /* Video sections (founder + testimonials) */
  .lps-{{ section.id }} .lps-video-block {
    padding: 4rem 0;
  }
  .lps-{{ section.id }} .lps-video-block--alt { background: #fff; }
  .lps-{{ section.id }} .lps-video-wrap {
    position: relative;
    border-radius: var(--lps-radius);
    overflow: hidden;
    background: #111;
    aspect-ratio: 16/9;
    max-width: 900px;
    margin: 0 auto;
    box-shadow: 0 12px 40px rgba(0,0,0,0.12);
  }
  .lps-{{ section.id }} .lps-video-wrap .lps-video-placeholder {
    --lps-vph-bg: #111111;
  }
  .lps-{{ section.id }} .lps-video-wrap iframe,
  .lps-{{ section.id }} .lps-video-wrap video {
    width: 100%;
    height: 100%;
    display: block;
    border: 0;
    object-fit: cover;
  }
  /* Empty video slot — label matches bg; stroke is a darker mix of the same hue */
  .lps-{{ section.id }} .lps-video-placeholder {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    --lps-vph-bg: #141414;
    background: var(--lps-vph-bg);
  }
  .lps-{{ section.id }} .lps-video-placeholder--card {
    --lps-vph-bg: #0d0d0d;
  }
  .lps-{{ section.id }} .lps-video-placeholder__text {
    font-size: clamp(0.82rem, 2.2vw, 1rem);
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--lps-vph-bg) 42%, #ffffff 58%);
    -webkit-text-stroke: 1.5px color-mix(in srgb, var(--lps-vph-bg) 12%, #000000 88%);
    paint-order: stroke fill;
    text-shadow:
      0 1px 0 color-mix(in srgb, var(--lps-vph-bg) 8%, #000000 92%),
      0 -1px 0 color-mix(in srgb, var(--lps-vph-bg) 8%, #000000 92%),
      1px 0 0 color-mix(in srgb, var(--lps-vph-bg) 8%, #000000 92%),
      -1px 0 0 color-mix(in srgb, var(--lps-vph-bg) 8%, #000000 92%);
  }
  @supports not (color: color-mix(in srgb, black, white)) {
    .lps-{{ section.id }} .lps-video-placeholder__text {
      color: rgba(255,255,255,0.38);
      -webkit-text-stroke: 1.5px #000000;
      text-shadow:
        0 1px 0 #000,
        0 -1px 0 #000,
        1px 0 0 #000,
        -1px 0 0 #000;
    }
  }
  .lps-{{ section.id }} .lps-vtesti__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1.25rem;
  }
  @media (min-width: 1100px) {
    .lps-{{ section.id }} .lps-vtesti__grid {
      grid-template-columns: repeat(auto-fill, minmax(210px, 1fr));
    }
  }
  /* Video row — marquee (no overflow scroll / no scrollbar) */
  .lps-{{ section.id }} .lps-vtesti__marquee {
    overflow: hidden;
    margin-top: 0.5rem;
    margin-left: -1.25rem;
    margin-right: -1.25rem;
    padding-left: 1.25rem;
    padding-right: 1.25rem;
  }
  .lps-{{ section.id }} .lps-vtesti__marquee-track {
    display: flex;
    width: max-content;
    gap: 1.25rem;
    animation: lps-gbp-marquee-scroll var(--lps-vtesti-dur, 60s) linear infinite;
    will-change: transform;
  }
  .lps-{{ section.id }} .lps-vtesti__marquee:hover .lps-vtesti__marquee-track {
    animation-play-state: paused;
  }
  .lps-{{ section.id }} .lps-vtesti__marquee-group {
    display: flex;
    gap: 1.25rem;
    flex-shrink: 0;
  }
  @media (prefers-reduced-motion: reduce) {
    .lps-{{ section.id }} .lps-vtesti__marquee-track {
      animation: none;
      flex-wrap: wrap;
      width: 100%;
      max-width: 100%;
      justify-content: center;
    }
    .lps-{{ section.id }} .lps-vtesti__marquee .lps-vtesti__marquee-group[aria-hidden="true"] {
      display: none;
    }
    .lps-{{ section.id }} .lps-vtesti__marquee-group {
      flex-wrap: wrap;
      justify-content: center;
    }
  }
  .lps-{{ section.id }} .lps-vtesti__marquee .lps-vtesti__card {
    flex: 0 0 auto;
    width: min(280px, 88vw);
    max-width: 320px;
  }
  .lps-{{ section.id }} .lps-vtesti__card {
    background: var(--lps-bg-warm);
    border: 1px solid rgba(139,69,19,0.12);
    border-radius: var(--lps-radius);
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }
  .lps-{{ section.id }} .lps-vtesti__embed {
    position: relative;
    aspect-ratio: 16/9;
    background: #000;
  }
  .lps-{{ section.id }} .lps-vtesti__embed iframe { width: 100%; height: 100%; border: 0; display: block; }
  .lps-{{ section.id }} .lps-vtesti__body { padding: 1.1rem 1.15rem 1.25rem; flex: 1; }
  .lps-{{ section.id }} .lps-vtesti__quote {
    font-size: 0.92rem;
    line-height: 1.55;
    margin: 0 0 0.65rem;
    font-style: italic;
    opacity: 0.88;
  }
  .lps-{{ section.id }} .lps-vtesti__name {
    font-weight: 700;
    font-size: 0.88rem;
    margin: 0;
    color: var(--lps-accent);
  }

  /* Google reviews */
  .lps-{{ section.id }} .lps-gbp {
    padding: 4rem 0;
    background: linear-gradient(180deg, #fff 0%, var(--lps-bg-warm) 100%);
  }
  .lps-{{ section.id }} .lps-gbp__embed {
    border-radius: var(--lps-radius);
    overflow: hidden;
    min-height: 120px;
  }
  .lps-{{ section.id }} .lps-gbp__manual {
    display: grid;
    gap: 1rem;
    margin-top: 2rem;
  }
  /* Manual reviews — infinite horizontal marquee (<marquee>-style) */
  .lps-{{ section.id }} .lps-gbp__marquee {
    overflow: hidden;
    margin-top: 2rem;
    margin-left: -1.25rem;
    margin-right: -1.25rem;
    padding-left: 1.25rem;
    padding-right: 1.25rem;
  }
  .lps-{{ section.id }} .lps-gbp__marquee-track {
    display: flex;
    width: max-content;
    gap: 1rem;
    animation: lps-gbp-marquee-scroll var(--lps-gbp-dur, 45s) linear infinite;
    will-change: transform;
  }
  .lps-{{ section.id }} .lps-gbp__marquee:hover .lps-gbp__marquee-track {
    animation-play-state: paused;
  }
  .lps-{{ section.id }} .lps-gbp__marquee-group {
    display: flex;
    gap: 1rem;
    flex-shrink: 0;
  }
  @keyframes lps-gbp-marquee-scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  @media (prefers-reduced-motion: reduce) {
    .lps-{{ section.id }} .lps-gbp__marquee-track {
      animation: none;
      flex-wrap: wrap;
      width: 100%;
      max-width: 100%;
      justify-content: center;
    }
    .lps-{{ section.id }} .lps-gbp__marquee .lps-gbp__marquee-group[aria-hidden="true"] {
      display: none;
    }
    .lps-{{ section.id }} .lps-gbp__marquee-group {
      flex-wrap: wrap;
      justify-content: center;
    }
  }
  .lps-{{ section.id }} .lps-gbp__review {
    padding: 1.15rem 1.25rem;
    background: #fff;
    border-radius: var(--lps-radius);
    border: 1px solid rgba(0,0,0,0.06);
    box-shadow: 0 2px 12px rgba(0,0,0,0.04);
    flex: 0 0 auto;
    width: min(100%, 300px);
    min-width: 260px;
    max-width: 320px;
  }
  .lps-{{ section.id }} .lps-gbp__manual .lps-gbp__review {
    width: auto;
    min-width: 0;
    max-width: none;
  }
  .lps-{{ section.id }} .lps-gbp__stars { letter-spacing: 2px; color: #e6a317; font-size: 0.9rem; margin-bottom: 0.35rem; }
  .lps-{{ section.id }} .lps-gbp__text { margin: 0; font-size: 0.92rem; line-height: 1.55; }
  .lps-{{ section.id }} .lps-gbp__meta { margin-top: 0.65rem; font-size: 0.78rem; opacity: 0.55; }

  /* Brand story */
  .lps-{{ section.id }} .lps-story {
    padding: 5rem 1.25rem;
  }
  .lps-{{ section.id }} .lps-story__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2.5rem;
    align-items: center;
  }
  .lps-{{ section.id }} .lps-story__img {
    border-radius: var(--lps-radius);
    overflow: hidden;
    aspect-ratio: 4/5;
    background: #eee;
  }
  .lps-{{ section.id }} .lps-story__img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .lps-{{ section.id }} .lps-story__body .lps-section-title { text-align: left; margin-bottom: 1.25rem; }
  .lps-{{ section.id }} .lps-story__rte { line-height: 1.75; opacity: 0.9; }
  @media (max-width: 900px) {
    .lps-{{ section.id }} .lps-story__grid { grid-template-columns: 1fr; }
    .lps-{{ section.id }} .lps-story__body .lps-section-title { text-align: center; }
  }

  /* Instagram grid */
  .lps-{{ section.id }} .lps-ig {
    padding: 4rem 0 5rem;
    background: #fff;
    border-top: 1px solid rgba(139,69,19,0.08);
  }
  .lps-{{ section.id }} .lps-ig__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 0.85rem;
  }
  @media (min-width: 700px) {
    .lps-{{ section.id }} .lps-ig__grid { grid-template-columns: repeat(4, 1fr); }
  }
  .lps-{{ section.id }} .lps-ig__item {
    position: relative;
    aspect-ratio: 9/16;
    border-radius: var(--lps-radius);
    overflow: hidden;
    display: block;
    text-decoration: none;
    color: inherit;
    border: 1px solid rgba(0,0,0,0.06);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
  }
  /* Feed-style square tiles (optional per block) */
  .lps-{{ section.id }} .lps-ig__item--square {
    aspect-ratio: 1;
  }
  .lps-{{ section.id }} .lps-ig__reel-badge {
    position: absolute;
    top: 0.45rem;
    right: 0.45rem;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 6px;
    background: rgba(0,0,0,0.55);
    color: #fff;
    pointer-events: none;
    box-shadow: 0 1px 4px rgba(0,0,0,0.25);
  }
  .lps-{{ section.id }} .lps-ig__reel-badge svg {
    width: 0.95rem;
    height: 0.95rem;
    display: block;
  }
  /* No cover image yet — Instagram / Reel placeholder (feeds preview) */
  .lps-{{ section.id }} .lps-ig__placeholder {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.4rem;
    padding: 1rem;
    text-align: center;
    background: linear-gradient(165deg, #1a0f0a 0%, #3d2415 42%, #6b3f24 100%);
  }
  .lps-{{ section.id }} .lps-ig__placeholder__label {
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.45);
  }
  .lps-{{ section.id }} .lps-ig__placeholder__text {
    font-size: clamp(0.78rem, 2.8vw, 1rem);
    font-weight: 800;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.28);
    -webkit-text-stroke: 1px rgba(0,0,0,0.35);
    paint-order: stroke fill;
  }
  .lps-{{ section.id }} .lps-ig__item:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 28px rgba(0,0,0,0.12);
  }
  .lps-{{ section.id }} .lps-ig__item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .lps-{{ section.id }} .lps-ig__cap {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.5rem 0.6rem;
    font-size: 0.68rem;
    line-height: 1.3;
    background: linear-gradient(transparent, rgba(0,0,0,0.75));
    color: #fff;
    max-height: 42%;
    overflow: hidden;
  }
{%- endstyle -%}

{%- liquid
  assign product = section.settings.featured_product
  assign vtesti_blocks = section.blocks | where: 'type', 'video_testimonial'
  assign ig_blocks = section.blocks | where: 'type', 'instagram_post'
  assign gbp_manual = section.blocks | where: 'type', 'gbp_review'
  assign hero_link = section.settings.hero_cta_link
  if hero_link == blank and product != blank
    assign hero_link = '#lps-product'
  elsif hero_link == blank
    assign hero_link = routes.all_products_collection_url
  endif
-%}

<div class="lps-{{ section.id }}">

  {%- comment -%} Top-left logo (same pattern as custom landing page) {%- endcomment -%}
  {%- assign banner_logo = section.settings.banner_logo | default: settings.logo -%}
  <div class="lps-corner-logo">
    {%- if banner_logo != blank -%}
      <img
        src="{{ banner_logo | image_url: width: 400 }}"
        alt="{{ shop.name }}"
        width="{{ banner_logo.width | default: 400 }}"
        height="{{ banner_logo.height | default: 120 }}"
        loading="eager"
      >
    {%- else -%}
      <img
        src="{{ 'logo.avif' | asset_url }}"
        alt="{{ shop.name }}"
        width="400"
        height="120"
        loading="eager"
      >
    {%- endif -%}
  </div>

  {%- comment -%} Brand title banner {%- endcomment -%}
  <div class="lps-brand-banner">
    <span class="lps-brand-banner__eyebrow">{{ section.settings.banner_eyebrow | default: 'The Art of Indian Elegance' | escape }}</span>
    <span class="lps-brand-banner__title">{{ section.settings.banner_title | default: 'Product title' | escape }}</span>
    <div class="lps-brand-banner__divider">
      <span class="lps-brand-banner__tagline">{{ section.settings.banner_tagline | default: 'Handcrafted · Heritage · Timeless' | escape }}</span>
    </div>
  </div>

  {%- comment -%} Hero {%- endcomment -%}
  <section class="lps-hero">
    {%- if section.settings.hero_image != blank -%}
      <img class="lps-hero__bg" src="{{ section.settings.hero_image | image_url: width: 2000 }}" alt="" width="2000" height="1200" loading="eager">
    {%- else -%}
      <div class="lps-hero__bg" style="background: linear-gradient(135deg, #5c3317 0%, #8b4513 50%, #c47020 100%);"></div>
    {%- endif -%}
    <div class="lps-hero__overlay"></div>
    <div class="lps-hero__content">
      {%- if section.settings.hero_eyebrow != blank -%}
        <p class="lps-hero__eyebrow">{{ section.settings.hero_eyebrow | escape }}</p>
      {%- endif -%}
      <h1 class="lps-hero__heading">{{ section.settings.hero_heading | escape }}</h1>
      {%- if section.settings.hero_subheading != blank -%}
        <p class="lps-hero__sub">{{ section.settings.hero_subheading | escape }}</p>
      {%- endif -%}
      {%- if section.settings.hero_proof != blank -%}
        <p class="lps-hero__proof">⭐ {{ section.settings.hero_proof | escape }}</p>
      {%- endif -%}
      {%- if section.settings.hero_cta_text != blank or section.settings.hero_cta2_text != blank -%}
        <div class="lps-hero__cta">
          {%- if section.settings.hero_cta_text != blank -%}
            <a href="{{ hero_link }}" class="lps-cta-primary">{{ section.settings.hero_cta_text | escape }}</a>
          {%- endif -%}
          {%- if section.settings.hero_cta2_text != blank -%}
            <a href="{{ section.settings.hero_cta2_link | default: hero_link }}" class="lps-cta-secondary">{{ section.settings.hero_cta2_text | escape }}</a>
          {%- endif -%}
        </div>
      {%- endif -%}
    </div>
  </section>

  {%- comment -%} Single product — card layout like custom landing product grid {%- endcomment -%}
  {%- if product != blank -%}
    <section class="lps-products" id="lps-product">
      <div class="lps-inner">
        <h2 class="lps-section-title">
          {%- if section.settings.product_section_heading != blank -%}
            {{ section.settings.product_section_heading | escape }}
          {%- else -%}
            {{ product.title | escape }}
          {%- endif -%}
        </h2>
        {%- if section.settings.product_section_subheading != blank -%}
          <p class="lps-section-sub">{{ section.settings.product_section_subheading | escape }}</p>
        {%- endif -%}

        <div class="lps-prod-grid lps-prod-grid--single">
          <article class="lps-prod-card">
            <div class="lps-prod-card__thumb">
              {%- if product.featured_media -%}
                <img
                  src="{{ product.featured_media | image_url: width: 900 }}"
                  alt="{{ product.featured_media.alt | default: product.title | escape }}"
                  width="900"
                  height="{{ 900 | divided_by: product.featured_media.aspect_ratio | round }}"
                  loading="lazy"
                >
              {%- endif -%}
              {%- if product.available == false -%}
                <div class="lps-prod-card__sold-out">Sold Out</div>
              {%- elsif product.compare_at_price > product.price -%}
                <span class="lps-prod-card__badge">Sale</span>
              {%- endif -%}
            </div>
            <div class="lps-prod-card__body">
              <p class="lps-prod-card__name">{{ product.title | escape }}</p>
              <p class="lps-prod-card__price">
                {%- if product.compare_at_price > product.price -%}
                  <s>{{ product.compare_at_price | money }}</s>
                {%- endif -%}
                {{ product.price | money }}
              </p>

              {%- if section.settings.show_product_description -%}
                <div class="lps-prod-card__desc rte">
                  {%- if section.settings.product_description_truncate -%}
                    {{ product.description | strip_html | truncatewords: 55 }}
                  {%- else -%}
                    {{ product.description }}
                  {%- endif -%}
                </div>
              {%- endif -%}

              {%- if product.available -%}
                {%- assign product_form_id = 'lps-product-form-' | append: section.id -%}
                {%- form 'product', product, id: product_form_id, is: 'product-form', class: 'lps-prod-card__form' -%}
                  <input type="hidden" name="quantity" value="1">
                  {%- if product.variants.size > 1 -%}
                    <label class="visually-hidden" for="{{ product_form_id }}-select">Variant</label>
                    <select name="id" id="{{ product_form_id }}-select">
                      {%- for variant in product.variants -%}
                        <option
                          value="{{ variant.id }}"
                          {% if variant == product.selected_or_first_available_variant %}selected{% endif %}
                          {% unless variant.available %}disabled{% endunless %}
                        >
                          {{ variant.title }} — {{ variant.price | money }}{% unless variant.available %} (Sold out){% endunless %}
                        </option>
                      {%- endfor -%}
                    </select>
                  {%- else -%}
                    <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                  {%- endif -%}
                  <button type="submit" class="button button--primary button--full lps-prod-card__btn" name="add">
                    {{ section.settings.product_cta_text | default: 'Add to cart' | escape }}
                  </button>
                {%- endform -%}
              {%- endif -%}

              <a href="{{ product.url }}" class="lps-prod-card__link">{{ section.settings.product_full_link_text | default: 'View full details' | escape }}</a>
            </div>
          </article>
        </div>
      </div>
    </section>
  {%- endif -%}

  {%- comment -%} Founder video {%- endcomment -%}
  {%- liquid
    assign show_founder_section = false
    if section.settings.founder_heading != blank or section.settings.founder_subheading != blank or section.settings.founder_embed != blank or section.settings.founder_video != blank
      assign show_founder_section = true
    endif
  -%}
  {%- if show_founder_section -%}
    <section class="lps-video-block lps-video-block--alt">
      <div class="lps-inner">
        {%- if section.settings.founder_heading != blank -%}
          <h2 class="lps-section-title">{{ section.settings.founder_heading | escape }}</h2>
        {%- endif -%}
        {%- if section.settings.founder_subheading != blank -%}
          <p class="lps-section-sub">{{ section.settings.founder_subheading | escape }}</p>
        {%- endif -%}
        <div class="lps-video-wrap">
          {%- if section.settings.founder_embed != blank -%}
            {{- section.settings.founder_embed -}}
          {%- elsif section.settings.founder_video != blank -%}
            {{ section.settings.founder_video | video_tag: image_size: '1100x', autoplay: true, loop: true, muted: true, controls: true }}
          {%- else -%}
            <div class="lps-video-placeholder" role="img" aria-label="Video">
              <span class="lps-video-placeholder__text">Video</span>
            </div>
          {%- endif -%}
        </div>
      </div>
    </section>
  {%- endif -%}

  {%- comment -%} Video testimonials {%- endcomment -%}
  {%- if vtesti_blocks.size > 0 -%}
    <section class="lps-video-block">
      <div class="lps-inner">
        <h2 class="lps-section-title">{{ section.settings.vtesti_heading | escape }}</h2>
        {%- if section.settings.vtesti_subheading != blank -%}
          <p class="lps-section-sub">{{ section.settings.vtesti_subheading | escape }}</p>
        {%- endif -%}
        {%- if section.settings.vtesti_horizontal_scroll -%}
          <div
            class="lps-vtesti__marquee"
            style="--lps-vtesti-dur: {{ section.settings.vtesti_marquee_seconds | default: 60 }}s;"
            role="region"
            aria-label="{{ section.settings.vtesti_heading | escape }}"
          >
            <div class="lps-vtesti__marquee-track">
              <div class="lps-vtesti__marquee-group">
                {%- for block in vtesti_blocks -%}
                  <article class="lps-vtesti__card" {{ block.shopify_attributes }}>
                    <div class="lps-vtesti__embed">
                      {%- if block.settings.embed != blank -%}
                        {{- block.settings.embed -}}
                      {%- else -%}
                        <div class="lps-video-placeholder lps-video-placeholder--card" role="img" aria-label="Video">
                          <span class="lps-video-placeholder__text">Video</span>
                        </div>
                      {%- endif -%}
                    </div>
                    <div class="lps-vtesti__body">
                      {%- if block.settings.quote != blank -%}
                        <p class="lps-vtesti__quote">“{{ block.settings.quote | escape }}”</p>
                      {%- endif -%}
                      {%- if block.settings.customer_name != blank -%}
                        <p class="lps-vtesti__name">{{ block.settings.customer_name | escape }}</p>
                      {%- endif -%}
                    </div>
                  </article>
                {%- endfor -%}
              </div>
              <div class="lps-vtesti__marquee-group" aria-hidden="true">
                {%- for block in vtesti_blocks -%}
                  <article class="lps-vtesti__card">
                    <div class="lps-vtesti__embed">
                      {%- if block.settings.embed != blank -%}
                        {{- block.settings.embed -}}
                      {%- else -%}
                        <div class="lps-video-placeholder lps-video-placeholder--card" role="img" aria-hidden="true">
                          <span class="lps-video-placeholder__text">Video</span>
                        </div>
                      {%- endif -%}
                    </div>
                    <div class="lps-vtesti__body">
                      {%- if block.settings.quote != blank -%}
                        <p class="lps-vtesti__quote">“{{ block.settings.quote | escape }}”</p>
                      {%- endif -%}
                      {%- if block.settings.customer_name != blank -%}
                        <p class="lps-vtesti__name">{{ block.settings.customer_name | escape }}</p>
                      {%- endif -%}
                    </div>
                  </article>
                {%- endfor -%}
              </div>
            </div>
          </div>
        {%- else -%}
          <div class="lps-vtesti__grid">
            {%- for block in vtesti_blocks -%}
              <article class="lps-vtesti__card" {{ block.shopify_attributes }}>
                <div class="lps-vtesti__embed">
                  {%- if block.settings.embed != blank -%}
                    {{- block.settings.embed -}}
                  {%- else -%}
                    <div class="lps-video-placeholder lps-video-placeholder--card" role="img" aria-label="Video">
                      <span class="lps-video-placeholder__text">Video</span>
                    </div>
                  {%- endif -%}
                </div>
                <div class="lps-vtesti__body">
                  {%- if block.settings.quote != blank -%}
                    <p class="lps-vtesti__quote">“{{ block.settings.quote | escape }}”</p>
                  {%- endif -%}
                  {%- if block.settings.customer_name != blank -%}
                    <p class="lps-vtesti__name">{{ block.settings.customer_name | escape }}</p>
                  {%- endif -%}
                </div>
              </article>
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>
    </section>
  {%- endif -%}

  {%- comment -%} Google Business Profile reviews (embed + optional manual) {%- endcomment -%}
  {%- if section.settings.gbp_heading != blank or section.settings.gbp_embed != blank or gbp_manual.size > 0 -%}
    <section class="lps-gbp">
      <div class="lps-inner">
        {%- if section.settings.gbp_heading != blank -%}
          <h2 class="lps-section-title">{{ section.settings.gbp_heading | escape }}</h2>
        {%- endif -%}
        {%- if section.settings.gbp_subheading != blank -%}
          <p class="lps-section-sub">{{ section.settings.gbp_subheading | escape }}</p>
        {%- endif -%}
        {%- if section.settings.gbp_embed != blank -%}
          <div class="lps-gbp__embed">
            {{- section.settings.gbp_embed -}}
          </div>
        {%- endif -%}
        {%- if gbp_manual.size > 0 -%}
          {%- if section.settings.gbp_reviews_marquee -%}
            <div
              class="lps-gbp__marquee"
              style="--lps-gbp-dur: {{ section.settings.gbp_marquee_seconds | default: 45 }}s;"
              role="region"
              aria-label="{{ section.settings.gbp_heading | default: 'Customer reviews' | escape }}"
            >
              <div class="lps-gbp__marquee-track">
                <div class="lps-gbp__marquee-group">
                  {%- for block in gbp_manual -%}
                    <div class="lps-gbp__review" {{ block.shopify_attributes }}>
                      {%- assign r = block.settings.rating | default: 5 -%}
                      <div class="lps-gbp__stars" aria-label="{{ r }} stars">
                        {%- for i in (1..r) -%}★{%- endfor -%}
                      </div>
                      {%- if block.settings.review_text != blank -%}
                        <p class="lps-gbp__text">{{ block.settings.review_text | escape }}</p>
                      {%- endif -%}
                      <div class="lps-gbp__meta">
                        {%- if block.settings.author != blank -%}
                          <span>{{ block.settings.author | escape }}</span>
                        {%- endif -%}
                        {%- if block.settings.review_date != blank -%}
                          <span> · {{ block.settings.review_date | escape }}</span>
                        {%- endif -%}
                      </div>
                    </div>
                  {%- endfor -%}
                </div>
                <div class="lps-gbp__marquee-group" aria-hidden="true">
                  {%- for block in gbp_manual -%}
                    <div class="lps-gbp__review">
                      {%- assign r = block.settings.rating | default: 5 -%}
                      <div class="lps-gbp__stars" aria-hidden="true">
                        {%- for i in (1..r) -%}★{%- endfor -%}
                      </div>
                      {%- if block.settings.review_text != blank -%}
                        <p class="lps-gbp__text">{{ block.settings.review_text | escape }}</p>
                      {%- endif -%}
                      <div class="lps-gbp__meta">
                        {%- if block.settings.author != blank -%}
                          <span>{{ block.settings.author | escape }}</span>
                        {%- endif -%}
                        {%- if block.settings.review_date != blank -%}
                          <span> · {{ block.settings.review_date | escape }}</span>
                        {%- endif -%}
                      </div>
                    </div>
                  {%- endfor -%}
                </div>
              </div>
            </div>
          {%- else -%}
            <div class="lps-gbp__manual">
              {%- for block in gbp_manual -%}
                <div class="lps-gbp__review" {{ block.shopify_attributes }}>
                  {%- assign r = block.settings.rating | default: 5 -%}
                  <div class="lps-gbp__stars" aria-label="{{ r }} stars">
                    {%- for i in (1..r) -%}★{%- endfor -%}
                  </div>
                  {%- if block.settings.review_text != blank -%}
                    <p class="lps-gbp__text">{{ block.settings.review_text | escape }}</p>
                  {%- endif -%}
                  <div class="lps-gbp__meta">
                    {%- if block.settings.author != blank -%}
                      <span>{{ block.settings.author | escape }}</span>
                    {%- endif -%}
                    {%- if block.settings.review_date != blank -%}
                      <span> · {{ block.settings.review_date | escape }}</span>
                    {%- endif -%}
                  </div>
                </div>
              {%- endfor -%}
            </div>
          {%- endif -%}
        {%- endif -%}
      </div>
    </section>
  {%- endif -%}

  {%- comment -%} Brand story {%- endcomment -%}
  {%- if section.settings.brand_story_heading != blank or section.settings.brand_story_text != blank -%}
    <section class="lps-story">
      <div class="lps-inner lps-story__grid">
        <div class="lps-story__img">
          {%- if section.settings.brand_story_image != blank -%}
            <img
              src="{{ section.settings.brand_story_image | image_url: width: 800 }}"
              alt="{{ section.settings.brand_story_image.alt | default: section.settings.brand_story_heading | escape }}"
              width="800"
              height="{{ 800 | divided_by: section.settings.brand_story_image.aspect_ratio | round }}"
              loading="lazy"
            >
          {%- elsif section.settings.hero_image != blank -%}
            <img
              src="{{ section.settings.hero_image | image_url: width: 800 }}"
              alt=""
              width="800"
              height="600"
              loading="lazy"
            >
          {%- else -%}
            <div style="width:100%;height:100%;min-height:280px;background:linear-gradient(145deg,#eee,#e5e0d8);" aria-hidden="true"></div>
          {%- endif -%}
        </div>
        <div class="lps-story__body">
          {%- if section.settings.brand_story_heading != blank -%}
            <h2 class="lps-section-title">{{ section.settings.brand_story_heading | escape }}</h2>
          {%- endif -%}
          <div class="lps-story__rte rte">{{ section.settings.brand_story_text }}</div>
        </div>
      </div>
    </section>
  {%- endif -%}

  {%- comment -%} Instagram / social posts (curated) {%- endcomment -%}
  {%- if ig_blocks.size > 0 -%}
    <section class="lps-ig">
      <div class="lps-inner">
        <h2 class="lps-section-title">{{ section.settings.ig_heading | escape }}</h2>
        {%- if section.settings.ig_subheading != blank -%}
          <p class="lps-section-sub">{{ section.settings.ig_subheading | escape }}</p>
        {%- endif -%}
        <div class="lps-ig__grid">
          {%- for block in ig_blocks -%}
            {%- assign ig_shape = block.settings.ig_tile_shape | default: 'reel' -%}
            {%- if block.settings.ig_show_reel_badge == false -%}
              {%- assign ig_show_badge = false -%}
            {%- else -%}
              {%- assign ig_show_badge = true -%}
            {%- endif -%}
            {%- assign ig_item_class = 'lps-ig__item' -%}
            {%- if ig_shape == 'square' -%}
              {%- assign ig_item_class = ig_item_class | append: ' lps-ig__item--square' -%}
            {%- endif -%}
            {%- capture ig_tile_inner -%}
              {%- if ig_shape == 'reel' and ig_show_badge -%}
                <span class="lps-ig__reel-badge" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                    <rect x="2" y="4" width="9" height="16" rx="1.5" stroke="currentColor" stroke-width="1.5"/>
                    <rect x="13" y="4" width="9" height="16" rx="1.5" stroke="currentColor" stroke-width="1.5" opacity="0.85"/>
                  </svg>
                </span>
              {%- endif -%}
              {%- if block.settings.cover_image != blank -%}
                <img
                  src="{{ block.settings.cover_image | image_url: width: 600 }}"
                  alt="{{ block.settings.caption | default: 'Instagram' | escape }}"
                  width="600"
                  height="{{ 600 | divided_by: block.settings.cover_image.aspect_ratio | round }}"
                  loading="lazy"
                >
              {%- else -%}
                <div
                  class="lps-ig__placeholder"
                  role="img"
                  aria-label="{% if ig_shape == 'square' %}Instagram post placeholder{% else %}Instagram Reel placeholder{% endif %}"
                >
                  <span class="lps-ig__placeholder__label">Instagram</span>
                  <span class="lps-ig__placeholder__text">{% if ig_shape == 'square' %}Post{% else %}Reel{% endif %}</span>
                </div>
              {%- endif -%}
              {%- if block.settings.caption != blank -%}
                <span class="lps-ig__cap">{{ block.settings.caption | escape }}</span>
              {%- endif -%}
            {%- endcapture -%}
            {%- if block.settings.post_link != blank -%}
              <a
                href="{{ block.settings.post_link }}"
                class="{{ ig_item_class }}"
                target="_blank"
                rel="noopener noreferrer"
                {{ block.shopify_attributes }}
              >
                {{- ig_tile_inner -}}
              </a>
            {%- else -%}
              <div class="{{ ig_item_class }}" {{ block.shopify_attributes }}>
                {{- ig_tile_inner -}}
              </div>
            {%- endif -%}
          {%- endfor -%}
        </div>
      </div>
    </section>
  {%- endif -%}

</div>

{% schema %}
{
  "name": "Story landing page",
  "tag": "section",
  "class": "section",
  "enabled_on": {
    "templates": ["page"]
  },
  "settings": [
    {
      "type": "paragraph",
      "content": "This section is the full story landing (banner, hero, product, videos, reviews, brand story, Instagram). Add blocks below for client videos, Google reviews, and Reels. Video placeholders disappear when you paste embeds or upload a file."
    },
    {
      "type": "header",
      "content": "Brand banner"
    },
    {
      "type": "image_picker",
      "id": "banner_logo",
      "label": "Logo",
      "info": "Top-left logo. Falls back to theme logo or logo.avif."
    },
    {
      "type": "text",
      "id": "banner_eyebrow",
      "label": "Banner eyebrow",
      "default": "The Art of Indian Elegance"
    },
    {
      "type": "text",
      "id": "banner_title",
      "label": "Banner title",
      "default": "Product title",
      "info": "Large gradient title below the logo. Clear the field to use the default “Product title”."
    },
    {
      "type": "text",
      "id": "banner_tagline",
      "label": "Banner tagline",
      "default": "Handcrafted · Heritage · Timeless"
    },
    {
      "type": "color",
      "id": "banner_color_start",
      "label": "Banner gradient start",
      "default": "#8b4513"
    },
    {
      "type": "color",
      "id": "banner_color_end",
      "label": "Banner gradient mid",
      "default": "#c47020"
    },
    {
      "type": "header",
      "content": "Hero"
    },
    {
      "type": "text",
      "id": "hero_eyebrow",
      "label": "Eyebrow (optional)",
      "default": "Handcrafted for you",
      "info": "Pill badge above the headline — hidden on small screens."
    },
    {
      "type": "text",
      "id": "hero_heading",
      "label": "Heading",
      "default": "Feel extraordinary in every celebration"
    },
    {
      "type": "textarea",
      "id": "hero_subheading",
      "label": "Subheading",
      "default": "Premium ethnic wear with honest craft, fast delivery, and a fit that flatters."
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Background image",
      "info": "Full-width lifestyle or product shot. If empty, a warm gradient is used."
    },
    {
      "type": "text",
      "id": "hero_cta_text",
      "label": "Primary button text",
      "default": "Shop the look"
    },
    {
      "type": "url",
      "id": "hero_cta_link",
      "label": "Primary button link",
      "info": "Defaults to the featured product section anchor if left blank."
    },
    {
      "type": "text",
      "id": "hero_proof",
      "label": "Social proof line (optional)",
      "default": "Loved by customers · 5★ rated",
      "info": "Shown under the subheading with a star. Leave blank to hide."
    },
    {
      "type": "text",
      "id": "hero_cta2_text",
      "label": "Secondary button text (optional)",
      "info": "Ghost-style button. Leave blank to hide."
    },
    {
      "type": "url",
      "id": "hero_cta2_link",
      "label": "Secondary button link"
    },
    {
      "type": "header",
      "content": "Featured product"
    },
    {
      "type": "product",
      "id": "featured_product",
      "label": "Product"
    },
    {
      "type": "text",
      "id": "product_section_heading",
      "label": "Section heading",
      "default": "Product title",
      "info": "Shown above the product card. Clear this field to use the selected product’s name from your catalog instead."
    },
    {
      "type": "text",
      "id": "product_section_subheading",
      "label": "Section subheading",
      "info": "Optional supporting line under the heading."
    },
    {
      "type": "checkbox",
      "id": "show_product_description",
      "label": "Show product description",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "product_description_truncate",
      "label": "Shorten description (first ~55 words)",
      "default": true,
      "info": "Turn off to show the full HTML description."
    },
    {
      "type": "text",
      "id": "product_cta_text",
      "label": "Add to cart button text",
      "default": "Add to cart"
    },
    {
      "type": "text",
      "id": "product_full_link_text",
      "label": "Link to full product page",
      "default": "View full details"
    },
    {
      "type": "header",
      "content": "Founder video"
    },
    {
      "type": "text",
      "id": "founder_heading",
      "label": "Heading",
      "default": "Meet the founder"
    },
    {
      "type": "text",
      "id": "founder_subheading",
      "label": "Subheading",
      "info": "Optional."
    },
    {
      "type": "video",
      "id": "founder_video",
      "label": "Video (upload)",
      "info": "Upload an MP4 to Shopify. Ignored if embed code below is filled."
    },
    {
      "type": "html",
      "id": "founder_embed",
      "label": "Video embed code (optional)",
      "info": "Paste a YouTube/Vimeo embed iframe here — takes priority over the uploaded video."
    },
    {
      "type": "header",
      "content": "Video testimonials"
    },
    {
      "type": "text",
      "id": "vtesti_heading",
      "label": "Heading",
      "default": "Hear from our clients"
    },
    {
      "type": "text",
      "id": "vtesti_subheading",
      "label": "Subheading"
    },
    {
      "type": "checkbox",
      "id": "vtesti_horizontal_scroll",
      "label": "Marquee (auto-scrolling row)",
      "default": true,
      "info": "Continuous horizontal animation — no scrollbar. Pause on hover. Turn off for a static grid."
    },
    {
      "type": "range",
      "id": "vtesti_marquee_seconds",
      "label": "Video row loop duration",
      "min": 30,
      "max": 120,
      "step": 5,
      "default": 60,
      "unit": "s",
      "info": "Seconds for one full cycle (lower = faster). Only applies when marquee is on."
    },
    {
      "type": "header",
      "content": "Google Business reviews"
    },
    {
      "type": "paragraph",
      "content": "Live syncing from Google requires a reviews app or widget (e.g. Elfsight, Tagembed). Paste the embed code below. You can add manual review blocks as a backup."
    },
    {
      "type": "text",
      "id": "gbp_heading",
      "label": "Heading",
      "default": "Google reviews"
    },
    {
      "type": "text",
      "id": "gbp_subheading",
      "label": "Subheading",
      "default": "What customers say on Google"
    },
    {
      "type": "html",
      "id": "gbp_embed",
      "label": "Reviews widget embed code",
      "info": "Paste the HTML/JS snippet from your Google Business reviews app."
    },
    {
      "type": "checkbox",
      "id": "gbp_reviews_marquee",
      "label": "Scrolling marquee for manual reviews",
      "default": true,
      "info": "Infinite horizontal scroll (like HTML marquee). Pause on hover. Respects “reduce motion” in the visitor’s system settings."
    },
    {
      "type": "range",
      "id": "gbp_marquee_seconds",
      "label": "Marquee loop duration",
      "min": 20,
      "max": 90,
      "step": 5,
      "default": 45,
      "unit": "s",
      "info": "Seconds for one full cycle (lower = faster)."
    },
    {
      "type": "header",
      "content": "Brand story"
    },
    {
      "type": "image_picker",
      "id": "brand_story_image",
      "label": "Image",
      "info": "Team, workshop, or lifestyle — optional; falls back to hero image."
    },
    {
      "type": "text",
      "id": "brand_story_heading",
      "label": "Heading",
      "default": "Our story"
    },
    {
      "type": "richtext",
      "id": "brand_story_text",
      "label": "Text",
      "default": "<p>We started with a simple belief: every woman deserves to feel confident in what she wears. Today we ship across the country, with every piece checked by hand.</p>"
    },
    {
      "type": "header",
      "content": "Instagram & reels"
    },
    {
      "type": "paragraph",
      "content": "Add one block per post or reel. Reels use a tall 9:16 cover and link to instagram.com/reel/…. Square tiles suit feed posts."
    },
    {
      "type": "text",
      "id": "ig_heading",
      "label": "Heading",
      "default": "Reels & posts"
    },
    {
      "type": "text",
      "id": "ig_subheading",
      "label": "Subheading",
      "default": "Tap a tile to open on Instagram.",
      "info": "Shown under the section title."
    }
  ],
  "blocks": [
    {
      "type": "video_testimonial",
      "name": "Video testimonial",
      "limit": 50,
      "settings": [
        {
          "type": "html",
          "id": "embed",
          "label": "Video embed",
          "info": "Paste the iframe from YouTube or Vimeo (Share → Embed)."
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Quote (optional)"
        },
        {
          "type": "text",
          "id": "customer_name",
          "label": "Customer name",
          "default": "Happy customer"
        }
      ]
    },
    {
      "type": "gbp_review",
      "name": "Manual Google review",
      "settings": [
        {
          "type": "text",
          "id": "author",
          "label": "Name",
          "default": "Verified customer"
        },
        {
          "type": "textarea",
          "id": "review_text",
          "label": "Review text"
        },
        {
          "type": "range",
          "id": "rating",
          "label": "Stars",
          "min": 1,
          "max": 5,
          "step": 1,
          "default": 5
        },
        {
          "type": "text",
          "id": "review_date",
          "label": "Date (optional)",
          "info": "e.g. March 2026"
        }
      ]
    },
    {
      "type": "instagram_post",
      "name": "Instagram / reel",
      "limit": 24,
      "settings": [
        {
          "type": "select",
          "id": "ig_tile_shape",
          "label": "Tile shape",
          "options": [
            { "value": "reel", "label": "Reel (9:16 vertical)" },
            { "value": "square", "label": "Feed post (1:1 square)" }
          ],
          "default": "reel",
          "info": "Match your cover image: tall for Reels, square for regular posts."
        },
        {
          "type": "checkbox",
          "id": "ig_show_reel_badge",
          "label": "Show Reels icon on tile",
          "default": true,
          "info": "Only applies to Reel (9:16) tiles. Hides for square posts."
        },
        {
          "type": "image_picker",
          "id": "cover_image",
          "label": "Cover image",
          "info": "Export a frame from your Reel (9:16) or a square crop for feed posts. Leave empty to show an Instagram / Reel placeholder tile."
        },
        {
          "type": "url",
          "id": "post_link",
          "label": "Instagram URL",
          "info": "Paste the reel or post link (e.g. instagram.com/reel/… or instagram.com/p/…). Opens in a new tab."
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Short label (optional)",
          "info": "Shown at the bottom of the tile."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Story landing page",
      "blocks": [
        {
          "type": "video_testimonial",
          "settings": {
            "quote": "The quality exceeded my expectations.",
            "customer_name": "Client name"
          }
        },
        {
          "type": "gbp_review",
          "settings": {
            "author": "Google reviewer",
            "review_text": "Beautiful pieces and fast delivery!",
            "rating": 5,
            "review_date": "2026"
          }
        },
        {
          "type": "instagram_post",
          "settings": {
            "ig_tile_shape": "reel",
            "ig_show_reel_badge": true
          }
        }
      ]
    }
  ]
}
{% endschema %}
